<h1 class="title">Between light and shadow</h1>
    <main>
    <ul>
    <% @users.each do |user| %>
        <li>
        <div class="card mb-3" style="max-width: 80%">
        <div class="row g-0">

    <div class="img-fluid rounded-start col-md-4"><%= image_tag (user.photo), height: 200 ,width: 200 %></div>
    <div class="col-md-8">
    <div class="card-body">
    <div class="card-text"><%= link_to (user.name),user_path(user.id) %></div>
            <button class="btn btn-warning home" >Number of posts: <%= user.posts_counter%></button>
            <%end%>
            </div>
            </div>
            </div>
            </div>
            </li>
    </ul>
    </main>

